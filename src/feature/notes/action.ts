export const favoriteAction = async (id: string) => {
    console.log(`${id} favorit action called`);
}

export const trashAction = async (id: string) => {
    console.log(`${id} trash action called`);
}

export const exportAction = async (id: string) => {
    console.log(`${id} export action called`);
}

export const restoreAction = async (id: string) => {
    console.log(`${id} restore action called`);
}