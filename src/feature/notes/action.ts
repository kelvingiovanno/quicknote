export const favoriteNoteAction = async (id: string) => {
    console.log(`${id} favorit note action called`);
}

export const trashNoteAction = async (id: string) => {
    console.log(`${id} trash note action called`);
}

export const exportNoteAction = async (id: string) => {
    console.log(`${id} export note action called`);
}

export const restoreNoteAction = async (id: string) => {
    console.log(`${id} restore note action called`);
}

export const removeNoteAction = async (id: string) => {
    console.log(`${id} remove note action called`);
}

export const updateNoteAction = async (id: string) => {
    console.log(`${id} restore note action called`);
}